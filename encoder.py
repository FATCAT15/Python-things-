#transforming input symbols to ASCII values
def cchrm():
    print(meenc[i], ord(meenc[i]))
def ordn():
    ord(meenc[i])
meenc = input(('Enter a message to encode: '))
for i in range(len(meenc)):
    cchrm()
#changing the ASCII value to an alternate number



    